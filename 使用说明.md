# M1597 Visual Index 使用说明

这是一个无需安装依赖的纯静态个人导航站。直接部署 `index.html`、`styles.css`、`script.js` 与图片资源即可使用。

## 日常使用

- 点击顶部的「搜索 / 日常 / 灵感 / 学习 / 休息」切换分区。
- 按 `/` 会返回首页并聚焦搜索框。
- 按 `Alt + 1` 到 `Alt + 5` 可直接跳到五个分区。
- 搜索框左侧可以选择 Bing、Google、百度或 DuckDuckGo；选择结果会保存在当前浏览器。
- 打开下方的网站后，首页会显示最近访问的 4 个网站。点击「清空」可删除这份记录。

## 修改导航链接

1. 点击右上角「管理链接」（手机端显示为「设置」）。
2. 选择需要修改的网站。
3. 修改名称、说明或网址，然后点击「保存修改」。
4. 「恢复当前」只重置所选网站；「恢复全部」重置所有网站。

这些修改使用浏览器 `localStorage` 保存，只在当前浏览器生效，不会上传，也不会同步到其他设备。清除浏览器站点数据后会恢复默认值。

如需让修改对所有访客永久生效，请直接编辑 `index.html` 中对应链接的 `href`、`<b>` 名称和 `<small>` 说明。

## 本地预览

直接双击 `index.html` 可以查看。为了与线上环境保持一致，也可以在当前目录运行：

```powershell
python -m http.server 4173
```

然后访问 `http://127.0.0.1:4173/`。

## 发布到 GitHub Pages

将整个目录提交到 `m1597.github.io` 仓库的发布分支即可。请保留所有 `.webp` 图片、JPEG 回退图片、`favicon.svg` 和 `og.jpg`。

如果网站最终地址不是 `https://m1597.github.io/`，需要同步修改 `index.html` 中的 `canonical`、`og:url`、`og:image` 和 `twitter:image` 地址。

## 图片维护

- 首页插画优先加载 `.webp`，旧浏览器自动回退到 `.jpg`。
- 更换插画时请同时生成同名 WebP，例如将 `hero.jpg` 对应为 `hero.webp`。
- 社交分享封面使用 `og.jpg`，推荐比例为 1200×630。
- 图片只应放在此目录内，并保持 HTML 中的文件名一致。
